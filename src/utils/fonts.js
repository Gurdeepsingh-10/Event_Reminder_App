import * as Font from 'expo-font';

export const loadFonts = async () => {
    // No fonts to load, return immediately
    return Promise.resolve();
};

export const fonts = {
    regular: 'System',
    medium: 'System',
    bold: 'System',
    semiBold: 'System',
};

